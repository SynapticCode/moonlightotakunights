/**
 * donate-config.js
 *
 * Sets window.MLN_STRIPE_LINK from a small inline config so we can
 * change the Stripe Payment Link URL without redeploying HTML.
 *
 * Replace the URL below with the live Payment Link from
 * Stripe Dashboard → Payment Links → "Moonlight Donate".
 *
 * Leave blank to disable the donate page (the form will show an error).
 */
window.MLN_STRIPE_LINK = '';
